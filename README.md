# book-finder-app
Book Finder is a React-based web app that lets users easily search for books using the Open Library API. It shows book covers, authors, publication years, and subjects in a responsive, user-friendly interface. The app includes clean error handling and ensures a smooth experience across all devices.
